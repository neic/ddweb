version: '3'

services:
  nginx:
    image: nginx:latest
    ports:
      - "8000:8000"
    volumes:
      - ./src:/src
      - ./config/nginx:/etc/nginx/conf.d
      - static:/static
    depends_on:
      - web
  web:
    build: .
    volumes:
      - .:/code
      - static:/static
    environment:
      - PGSQL_HOST=db
    depends_on:
      - db
  db:
    image: postgres

volumes:
  static:
